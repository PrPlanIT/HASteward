# CLI Reference

Every `hasteward` command and flag, generated from the CLI itself — never hand-edited.
The same content is available at the terminal via `hasteward <command> --help`.

--8<-- "assets/modules/cli-reference.md"
